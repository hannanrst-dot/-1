# راهنمای انتشار روی لیارا

## گام ۱ — ساخت برنامه در لیارا

۱. وارد پنل لیارا شوید → **برنامه‌ها** → **ایجاد برنامه**
۲. پلتفرم: **Node.js**
۳. یک نام انتخاب کنید (مثلاً `iphone-center`) و برنامه را بسازید.

## گام ۲ — ساخت دو دیسک (مهم — قبل از استقرار انجام دهید)

سایت روی دیتابیس SQLite کار می‌کند و تصاویر محصولات را روی دیسک ذخیره می‌کند.
بدون دیسک، با هر بار ری‌استارت همه‌چیز پاک می‌شود.

در صفحه برنامه → تب **دیسک‌ها** → دو دیسک بسازید:

| نام دیسک | مسیر اتصال | حجم پیشنهادی |
|---|---|---|
| `data` | `/app/data` | ۱ گیگابایت |
| `uploads` | `/app/public/uploads` | ۵ گیگابایت |

> نام دیسک‌ها باید **دقیقاً** `data` و `uploads` باشد چون در `liara.json` همین نام‌ها آمده است.

## گام ۳ — تنظیم متغیرهای محیطی

در صفحه برنامه → تب **متغیرها** این دو مورد را اضافه کنید:

| نام متغیر | مقدار |
|---|---|
| `DATABASE_URL` | `file:/app/data/prod.db` |
| `AUTH_SECRET` | یک رشته تصادفی طولانی (حداقل ۳۲ کاراکتر) |

برای ساخت مقدار `AUTH_SECRET` می‌توانید در ترمینال بزنید:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

> `AUTH_SECRET` کلید امضای ورود کاربران است. حتماً مقدار پیش‌فرض را عوض کنید و
> آن را در اختیار کسی قرار ندهید.

## گام ۴ — استقرار

اگر CLI لیارا را ندارید، یک بار نصب کنید:

```bash
npm install -g @liara/cli
liara login
```

سپس داخل پوشه پروژه:

```bash
liara deploy
```

لیارا از روی `liara.json` تشخیص می‌دهد که پلتفرم Node.js است، پورت ۳۰۰۰ را باز می‌کند
و دیسک‌ها را وصل می‌کند.

## گام ۵ — اولین ورود

پس از پایان استقرار، آدرس برنامه را باز کنید. دیتابیس در اولین اجرا به‌صورت
خودکار ساخته و با داده‌های نمونه پر می‌شود.

با این حساب وارد پنل مدیریت شوید:

```
admin@shop.ir  /  admin1234
```

**بلافاصله پس از اولین ورود، رمز عبور را از مسیر «حساب کاربری ← اطلاعات حساب» عوض کنید.**

---

## نکات مهم

### داده‌های نمونه پاک نمی‌شوند
اسکریپت داده‌های اولیه هوشمند است: اگر دیتابیس از قبل داده داشته باشد، هیچ کاری
نمی‌کند. یعنی ری‌استارت و استقرار مجدد، محصولات و سفارش‌های واقعی شما را پاک نمی‌کند.

اگر **عمداً** خواستید همه‌چیز را به حالت اولیه برگردانید، در ترمینال لیارا بزنید:

```bash
FORCE_SEED=1 npm run db:seed
```

### پاک کردن محصولات نمونه
پس از راه‌اندازی، از پنل مدیریت وارد بخش «تأیید محصولات» شوید و محصولات نمونه را
حذف یا مخفی کنید، سپس محصولات واقعی خود را از پنل فروشنده بارگذاری کنید.

### تغییر اطلاعات فروشگاه
نام فروشگاه، شعار، تلفن، آدرس و ایمیل همگی در فایل `src/lib/site.ts` هستند.
پس از تغییر، دوباره `liara deploy` بزنید.

### دامنه اختصاصی
در صفحه برنامه → تب **دامنه‌ها** → دامنه خود را اضافه و رکورد DNS را طبق راهنمای
لیارا تنظیم کنید. گواهی SSL به‌صورت خودکار صادر می‌شود.

### پشتیبان‌گیری
دیتابیس یک فایل تکی در مسیر `/app/data/prod.db` است. برای گرفتن نسخه پشتیبان
می‌توانید از ترمینال لیارا آن را دانلود کنید. توصیه می‌شود هفتگی این کار را انجام دهید.

---

## اگر بعداً ترافیک زیاد شد

SQLite برای یک فروشگاه با ترافیک متوسط کاملاً کافی است. اگر روزی نیاز به
PostgreSQL پیدا کردید:

۱. در لیارا یک دیتابیس PostgreSQL بسازید.
۲. در `prisma/schema.prisma` مقدار `provider = "sqlite"` را به `"postgresql"` تغییر دهید.
۳. متغیر `DATABASE_URL` را با رشته اتصال PostgreSQL جایگزین کنید.
۴. دوباره `liara deploy` بزنید.
