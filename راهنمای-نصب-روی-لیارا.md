# راهنمای بالا آوردن برنامه روی لیارا (Liara) — خیلی ساده

> چرا نت‌لیفای کار نکرد؟ چون نت‌لیفای فقط سایت‌های ساده (HTML) را اجرا می‌کند.
> این برنامه یک «سرور» زنده لازم دارد. لیارا این کار را انجام می‌دهد. ✅

این برنامه طوری تنظیم شده که روی لیارا **به‌محض بالا آمدن، خودش آماده شود** (کاربر
مدیر و کالاهای نمونه به‌صورت خودکار ساخته می‌شوند). فقط باید آن را آپلود کنید.

---

## روش ۱: با برنامهٔ خط فرمان لیارا (ساده‌ترین)

۱. در سایت **liara.ir** ثبت‌نام کنید و وارد شوید.

۲. از پنل لیارا یک **App جدید** بسازید:
   - روی «ایجاد برنامه» بزنید.
   - نوع (پلتفرم) را **Node.js** انتخاب کنید.
   - یک نام برای برنامه بگذارید (مثلاً `my-shop`).

۳. روی کامپیوترتان، ابزار لیارا را نصب کنید (یک‌بار):
   ```bash
   npm install -g @liara/cli
   ```

۴. وارد حساب لیارا شوید:
   ```bash
   liara login
   ```

۵. داخل پوشهٔ همین پروژه (جایی که فایل `package.json` هست) این دستور را بزنید:
   ```bash
   liara deploy --app my-shop --port 3000
   ```
   (به‌جای `my-shop` همان نامی را بگذارید که در مرحلهٔ ۲ ساختید.)

۶. بعد از چند دقیقه، لیارا یک آدرس به شما می‌دهد مثل:
   `https://my-shop.liara.run`
   آن را باز کنید. صفحهٔ ورود می‌آید.

۷. وارد شوید:
   - نام کاربری: `admin`
   - رمز: `admin`

   بعد از ورود، حتماً از **تنظیمات ← کاربران** رمز را عوض کنید.

---

## روش ۲: بدون خط فرمان (آپلود فایل از پنل)

اگر با خط فرمان راحت نیستید:

۱. در پنل لیارا App از نوع **Node.js** بسازید.
۲. از قسمت **«استقرار» یا «Deploy»** برنامه، گزینهٔ آپلود پوشه/فایل را بزنید و
   **همهٔ فایل‌های پروژه** (نه فایل zip، بلکه محتوای داخل آن) را آپلود کنید.
   > نکته: پوشهٔ `node_modules` را آپلود نکنید؛ لیارا خودش نصب می‌کند.
۳. منتظر بمانید تا وضعیت «سبز/در حال اجرا» شود، سپس آدرس برنامه را باز کنید.

---

## مهم: ماندگاری اطلاعات (برای استفادهٔ واقعی) — حتماً بخوانید

فضای فایل برنامه روی لیارا **فقط‌خواندنی** است، بنابراین برنامه به‌طور خودکار
دیتابیس را در یک مسیر موقت می‌سازد تا حتماً بالا بیاید. اما این مسیر موقت با هر
ری‌استارت/دیپلوی **پاک می‌شود** و اطلاعات به حالت نمونه برمی‌گردد.

برای اینکه اطلاعات شما (کالاها و فاکتورها) **همیشه بماند**، یک «دیسک» بسازید:

۱. در پنل لیارا، وارد برنامه شوید → از منوی کناری بخش **«دیسک‌ها» (Disks)**.
۲. روی «ایجاد دیسک» بزنید و مقادیر زیر را وارد کنید:
   - **نام دیسک:** `data`
   - **مسیر اتصال (Mount Path):** `/app/data`
   - حجم: ۱ گیگابایت کافی است.
۳. حالا در بخش **«متغیرها / تنظیمات محیطی» (Environment Variables)** برنامه، این
   دو متغیر را اضافه کنید تا دیتابیس و پشتیبان‌ها روی همان دیسک ذخیره شوند:
   - `DATABASE_FILE` = `/app/data/store.db`
   - `BACKUP_DIR` = `/app/data/backups`
۴. برنامه را یک‌بار دیگر ری‌استارت کنید.

از این پس اطلاعات شما روی دیسک ذخیره می‌شود و با ری‌استارت پاک نمی‌شود.
(همچنین از **تنظیمات ← پشتیبان‌گیری** داخل برنامه می‌توانید هر زمان یک نسخهٔ
پشتیبان بگیرید و دانلود کنید.)

> نکتهٔ فنی: برنامه هوشمند است — اگر دیسک را بسازید و متغیرها را ست کنید، خودکار
> از دیسک استفاده می‌کند؛ اگر نسازید، در مسیر موقت اجرا می‌شود تا حداقل بالا بیاید.

---

## اگر باز هم بالا نیامد چه کنم؟

- مطمئن شوید نوع برنامه را **Node.js** انتخاب کرده‌اید (نه Static و نه PHP).
- در بخش **«لاگ‌ها» (Logs)** برنامه در پنل لیارا نگاه کنید؛ اگر خطایی بود، متنش را
  کپی کنید و همان را به من بدهید تا دقیق راهنمایی کنم.
- آدرس برنامه حتماً با `https://` باز شود (برای کار کردن میکروفون لازم است).
